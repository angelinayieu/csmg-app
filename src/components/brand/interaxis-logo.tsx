/**
 * Back-compat shim. The brand is now akiboe — see `akiboe-logo.tsx`.
 * `InterAxisLogo` is kept as an alias so existing square logo slots keep
 * rendering (now the theme-aware akiboe sun mark).
 */
export { AkiboeLogo, AkiboeLogo as InterAxisLogo } from "./akiboe-logo";
